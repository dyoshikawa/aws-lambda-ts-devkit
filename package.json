{
  "name": "test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn run tsc index.ts",
    "format": "yarn run prettier --config .prettierrc index.ts --write"
  },
  "devDependencies": {
    "prettier": "^1.12.1",
    "typescript": "^2.8.3"
  }
}
